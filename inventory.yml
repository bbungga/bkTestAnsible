---
all:
  children:
    database: {}
    tower:
      hosts:
        localhost:
          admin_password: awxpass
          ansible_connection: local
          pg_database: awx
          pg_host: postgres
          pg_password: awxpass
          pg_port: '5432'
          pg_username: awx
    ungrouped: {}